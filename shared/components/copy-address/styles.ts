import styled from 'styled-components';

export const CopyButton = styled.button`
  display: flex;
  align-items: center;
  padding: 0;
  margin: 0;
  outline: none;
  border: none;
  background: transparent;
  cursor: pointer;
  appearance: none;
`;
