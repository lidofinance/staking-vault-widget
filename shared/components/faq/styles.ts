import styled from 'styled-components';
import { AccordionNavigatable } from './accordion-navigatable';

export const AccordionStyled = styled(AccordionNavigatable)`
  font-size: 12px;
  font-weight: 400;
`;
