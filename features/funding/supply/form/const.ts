import { config } from 'config';

const { docsOrigin } = config;

export const NOIdentificationLink = `${docsOrigin}/run-on-lido/stvaults/operational-and-management-guides/node-operators-identification`;
export const pdgLink = `${docsOrigin}/run-on-lido/stvaults/tech-documentation/pdg#pdg-shortcut`;
