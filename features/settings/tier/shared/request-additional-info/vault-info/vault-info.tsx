import { BaseMetrics } from '../../vault-metrics/base-metrics';

import { Wrapper } from './styles';

export const VaultInfo = () => {
  return (
    <Wrapper>
      <BaseMetrics />
    </Wrapper>
  );
};
